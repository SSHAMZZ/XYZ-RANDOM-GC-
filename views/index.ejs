<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Homepage</title>
    <link rel="stylesheet" href="/home-style.css">
    <!--Font preconnect-->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Libre+Baskerville&display=swap" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Noto+Sans:ital,wght@0,100..900;1,100..900&display=swap" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Space+Mono:ital,wght@0,400;0,700;1,400;1,700&display=swap" rel="stylesheet">
    <!-- Fonts awesome preconnect -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" integrity="sha512-DTOQO9RWCH3ppGqcWaEA1BIZOC6xxalwEsw9c2QQeAIftl+Vegovlnee1c9QX4TctnWMn13TZye+giMm8e2LwA==" crossorigin="anonymous" referrerpolicy="no-referrer" />
</head>
<body>
    <div class="main-heading">
        <div class="brand">
            <h1 class="heading">"XYZ"</h1>
            <p class="tagline">Your Random GC</p>
        </div>
        <div class="user-account">
            <a href="http://localhost:8080/profile">
                <div class="user-info">
                    <img src="<%= pfpImage.imageSrc %>" class = "pfp" alt="Pfp">
                    <p> <%= user.username %></p>
                </div>
            </a>
            
        </div>
    </div>
    
  
    <button class="new-post-button maroon "><a href="http://localhost:8080/posts/new"><i class="fa-solid fa-plus"></i> Create new</a></button>
    <% for (post of posts){ %>
        <div class="post">
            <h3 class="user">@<%= post.username %></h3>
            <p class="content"><%= post.content %></p>
          
            <button class="post-detail-button maroon"><a href="http://localhost:8080/posts/<%= post.id %>">More Details</a></button>
           
            <button class="post-edit-button maroon" ><a href="http://localhost:8080/posts/<%= post.id %>/edit"><i class="fa-regular fa-pen-to-square button-sizing"></i></a></button>

            <form method="post" action="/posts/<%= post.id %>?_method=DELETE" >
             <button class="post-delete-button maroon"><i class="fa-solid fa-trash button-sizing"></i></button>
            
           </form>
           <p class="time"><%= post.time %></p>
           <% if (post.editStatus == 1) {%>
            <p class="edited">(edited)</p>
            <% } %>
        </div>
    <% } %>
    
   
    <br><br><br>
    <div class="footer">
        <hr>
        <p class="disclaimer">
           XYZ &trade;
        </p>
    </div>
</body>
</html>